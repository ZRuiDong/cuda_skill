# 示例与边界情况

## 示例验证

### 图1.15(a)
P = x₁x₂ + x₁x₃x₄ + x₁x₃x₅ 
= x₁[x₂ + x₃(x₄ + x₅)] → 可因式分解 → **简单任务图**

### 图1.15(d)
P = x₁x₃ + x₁x₄ + x₂x₄ 
→ 无法提取统一公因子 → **非简单任务图**

## 边界情况说明
- 若图中仅有一条链，则 P(G) 为单项式，可视为已因式分解，判定为简单任务图。
- 若多条链完全无公共任务节点，则通常无法因式分解，判定为非简单任务图。
- 因式分解需基于标准代数规则，不依赖数值赋值或启发式近似。